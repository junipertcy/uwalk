commands:
	npm_install_bower:
		command: sudo npm install -g bower
container_commands:
	bower_install:
		command: sudo bower install --allow-root
	npm_install:
		cd $EB_CONFIG_APP_CURRENT; sudo npm install